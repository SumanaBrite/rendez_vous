<form method="post" action="{{ path('money_transfert_delete', {'id': money_transfert.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ money_transfert.id) }}">
    <button class="btn">Delete</button>
</form>
