{% extends 'base.html.twig' %}

{% block title %}Horaire{% endblock %}

{% block body %}
    <h1>Horaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ horaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ horaire.nom }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('horaire_index') }}">back to list</a>

    <a href="{{ path('horaire_edit', {'id': horaire.id}) }}">edit</a>

    {{ include('horaire/_delete_form.html.twig') }}
{% endblock %}
