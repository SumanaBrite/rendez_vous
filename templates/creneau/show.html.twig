{% extends 'base.html.twig' %}

{% block title %}Creneau{% endblock %}

{% block body %}
    <h1>Creneau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ creneau.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ creneau.description }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('creneau_index') }}">back to list</a>

    <a href="{{ path('creneau_edit', {'id': creneau.id}) }}">edit</a>

    {{ include('creneau/_delete_form.html.twig') }}
{% endblock %}
