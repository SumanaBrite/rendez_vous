{% extends 'base.html.twig' %}

{% block title %}JourFermetureGuichet index{% endblock %}

{% block body %}
    <h1>JourFermetureGuichet index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Jour</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for jour_fermeture_guichet in jour_fermeture_guichets %}
            <tr>
                <td>{{ jour_fermeture_guichet.id }}</td>
                <td>{{ jour_fermeture_guichet.jour ? jour_fermeture_guichet.jour|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('jour_fermerguichet_show', {'id': jour_fermeture_guichet.id}) }}">show</a>
                    <a href="{{ path('jour_fermerguichet_edit', {'id': jour_fermeture_guichet.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('jour_fermerguichet_new') }}">Create new</a>
{% endblock %}
