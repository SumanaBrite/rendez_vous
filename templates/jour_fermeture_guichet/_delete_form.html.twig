<form method="post" action="{{ path('jour_fermerguichet_delete', {'id': jour_fermeture_guichet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jour_fermeture_guichet.id) }}">
    <button class="btn">Delete</button>
</form>
